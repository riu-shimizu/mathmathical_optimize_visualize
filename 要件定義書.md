# 数理最適化 “探索プロセス可視化” Webアプリ 要件定義書（ドラフト）

## 1. システム概要

本システムは、**TSP（巡回セールスマン問題）**および**最小シュタイナー木問題**を題材に、
数理最適化アルゴリズムの **探索プロセスを「かっこよく」可視化する Web アプリケーション**である。

探索の進行に応じて、

* 問題グラフ（ノード・エッジ）
* 分枝限定法などによる探索木
* 評価値・ギャップなどの指標

を連動して表示し、**分枝限定のノード展開や解の遷移が直感的・視覚的に理解できる**ことを目的とする。

UI は **ダークテーマ + グラスモーフィズム** を採用し、
ノード展開時に探索木がなめらかに伸び、先端が光るような演出を重視する。

---

## 2. 目的・コンセプト

1. **アルゴリズムの直感的理解**

   * 分枝限定法や探索木ベースの探索が、
     「どのようにノードを増やし」「どこを枝刈りするか」を**視覚的に体験**できる。

2. **数理最適化への興味喚起**

   * 「かっこいい動き」とビジュアル重視のUIにより、
     数理最適化・探索アルゴリズムへのモチベーションを高める。

3. **教育向けデモツール**

   * 授業や演習で教員が投影して説明に使える。
   * また学生自身が触って遊びながら理解を深められる。

---

## 3. ターゲットユーザー・利用シーン

### 3.1 ターゲットユーザー

* 主対象：**情報系学部 1〜2 年生**

  * アルゴリズム入門レベル
  * グラフ・最短路・探索などを学び始める段階を想定

### 3.2 利用シーン

* 大学の授業で、教員が**プロジェクタに投影**して

  * 「分枝限定がどう進むか」をリアルタイムで見せる
* 学生が自習で

  * 自分でノードを配置して、探索過程を眺める
  * TSP とシュタイナー木の違いを感覚的に掴む

---

## 4. 対象問題・規模

### 4.1 対象問題

* **巡回セールスマン問題（TSP）**

  * 完全グラフ（ユークリッド距離ベース）を基本とする
  * ノードは平面上の点として表示

* **最小シュタイナー木問題**

  * 与えられた「端点（ターミナル）」に加え、
    任意のステイナーポイントを含み得るネットワークの最小化

### 4.2 想定規模

* TSP：ノード数 約 **10〜30** 個程度
* シュタイナー木：ターミナル＋ステイナーポイント合計で **20〜40** ノード程度

※教育用・可視化重視のため、**中規模以下に限定**する。

---

## 5. 機能要件

### 5.1 問題インスタンス編集機能（必須）

* キャンバス上にノードを表示（2D平面）
* 以下の操作をGUIで実現する：

  * ノードの追加（クリック、またはボタン＋クリック）
  * ノードの削除
  * ノードのドラッグ＆ドロップによる位置変更
  * TSP / シュタイナー木のモード切替

    * シュタイナー木モードでは、「ターミナルノード」とそれ以外を区別して表示（色・アイコンの差別化）
* 自動生成機能（任意）

  * ランダムにノードを散布するボタン
  * 「小 / 中 / 大」程度の難易度プリセット

### 5.2 探索実行・ログ生成

* 「探索開始」ボタン押下でソルバーを起動し、
  探索過程の**イベントログ**を生成する。

* ログに含める **重要イベント**（主にこれのみを記録する）

  * 新しい**ベスト可行解**が見つかった
  * 探索木ノードが展開された（分枝）
  * 探索ノードが枝刈りされた（下界がベストより悪い、等）
  * 探索のフェーズ切り替え（任意：例）root 緩和 → 分枝フェーズなど

* 探索は基本的に **バックエンドで一括実行し、その結果ログをフロント側で再生**する方式とする。

  * 処理が間に合う範囲では、逐次ログ送信による簡易リアルタイム表示も検討可（要設計）。

### 5.3 可視化ビュー

#### 5.3.1 グラフビュー（問題・解の表示）

* TSP モード

  * ノード：円 or 点で表示
  * エッジ：距離に応じた線（基本は全エッジ非表示 or 薄く表示）
  * 現在の**ベスト巡回路**を太線・発光表現で表示
  * 解更新イベント時に、**ルートがなめらかに変形するアニメーション**

* シュタイナー木モード

  * ターミナルノードを強調表示
  * ステイナーポイントを別のスタイルで表示
  * 現在の**ベスト木構造**を発光・なめらかな伸びでアニメーション表示

#### 5.3.2 探索木ビュー（最重要）

* 分枝限定の探索木を**2D ツリーレイアウト**で表示

  * ルートノードから下に向かってノードを展開
  * 新規ノード展開時：

    * ノードが**なめらかに伸びて出現**する（スライド・スケールインなど）
    * 先端ノード（現在展開中ノード）が**光る演出**（グロー、パルスアニメーション）
  * 枝刈りされたノード：

    * 色を変える（グレーアウト）
    * 透過度を上げる などの表現で「死んだ枝」を直感的に表現

* ズーム / パン操作（必須）

  * 探索木全体を俯瞰したり、局所を拡大できる

* 現在再生中のイベントに対応するノードをハイライト表示

#### 5.3.3 メトリクスビュー（補助的）

* 時間（イベントステップ）に対する以下の推移グラフを表示：

  * 目的関数値（ベスト解）
  * 下界（分枝限定の下界）
  * ギャップ（上界 - 下界）
  * 可能なら制約違反度（CP-SATの場合は主に可行・不可能の状態）

* 探索木ビュー・グラフビューのタイムラインと同期し、
  再生位置に縦線（カーソル）を表示。

### 5.4 タイムライン操作（必須）

* 探索ログを「アニメーション再生」するためのタイムラインUI：

  * 再生 / 一時停止
  * 巻き戻し（先頭へ / 数ステップ戻す）
  * 早送り（最後へ / 数ステップ進める）
  * スロー再生（再生速度 0.25x / 0.5x / 1x / 2x など）
  * スライダーで任意のイベント位置へジャンプ

* タイムライン位置に応じて：

  * グラフビュー：その時点のベスト解を表示
  * 探索木ビュー：展開済みノードのみ表示
  * メトリクスビュー：該当ステップまでの履歴を表示

### 5.5 アルゴリズム・ソルバー

* v1 で最低限サポートするもの：

  * **CP-SAT ソルバー（OR-Tools想定）による TSP / シュタイナー木の定式化と探索**
  * 分枝限定のノード展開・枝刈りの情報をログとして抽出

* 将来拡張を見据えた、アルゴリズム選択UI枠だけ用意：

  * アルゴリズム選択ドロップダウン：

    * [CP-SAT]（v1ではこれのみアクティブ）
    * [局所探索 / 焼きなまし]（グレーアウト or Coming Soon）
    * [遺伝的アルゴリズム]（同上）

---

## 6. UI / デザイン要件

* テーマ：

  * **ダークテーマ**をベース
  * ウィンドウ・パネル・カードに**グラスモーフィズム**（半透明・ぼかし）を適用
* 色・演出：

  * 探索木の先端ノードは**発光（グロー）**＋**なめらかなパルスアニメーション**
  * 解更新時に、グラフビューのルート or 木構造が**スムーズに遷移**
  * 過去ノードは控えめな色、現在注目ノードは鮮やかな色でコントラスト
* 操作感：

  * ノードのドラッグ&ドロップが滑らかに動く
  * タイムライン操作に対して、表示が**即座に追従**する
* 言語：

  * UI 表示は **日本語**

    * 例）「探索開始」「一時停止」「再生速度」「探索木」「目的関数値」など
  * 数学記号・略語（LB, UB, gapなど）は英字併記可

---

## 7. 非機能要件

### 7.1 パフォーマンス

* ノード数 30 程度・探索ログが中規模（数千イベント程度）の場合でも、

  * 再生がカクつかないこと（**体感でぬるぬる動く**こと）
* タイムラインスライド操作に対して、
  UI更新のレイテンシを短く（目標：0.2秒未満）保つ。

### 7.2 実行環境

* **Webブラウザ上で動作するクライアントサイドアプリ**を基本とする。
* ソルバーは

  * まずはバックエンド（例：Python + OR-Tools）で実行し、ログをフロントへ渡す構成を想定。
* ローカル環境での利用（開発者PC or 学内サーバ）を前提とし、

  * 認証・ユーザ管理などは v1 では実装しない。

### 7.3 保存・共有機能

* v1では**永続的な保存・共有機能は実装しない**。

  * ページをリロードすると状態はリセットでよい。
  * ただし「同一セッション内でのやり直し（タイムライン操作）」は可能。

---

## 8. 拡張性・将来拡張

* 将来的に以下の拡張を行えるよう、設計上の余地を残す：

  * 複数アルゴリズムの**挙動比較（並列表示）**

    * 例：CP-SAT vs 焼きなまし vs 遺伝的アルゴリズム
  * 探索木レイアウトや可視化テーマの切替
  * 問題インスタンス・探索ログの保存と共有（URLつきリプレイ）
  * 英語UI対応
  * グラフ以外の問題クラス（スケジューリングなど）への拡張

---

## 9. 参考となる技術スタック（例：要件には含めず）

※実装案としての例示（要件として固定はしない）

* フロントエンド：

  * TypeScript + React
  * グラフ描画：D3.js / PixiJS / WebGLベースライブラリ
* バックエンド：

  * Python + FastAPI
  * OR-Tools（CP-SAT）で TSP / シュタイナー木を解く
* 通信：

  * REST API or WebSocket（リアルタイム性が必要な場合）
